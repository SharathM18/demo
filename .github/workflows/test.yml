name: env

on:
  push:
    branches: ["main"]

jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - run: echo "env ${{ secrete.API_KEY }}"
      - run: echo "env $token"
    env:
      token: ${{ secrete.API_KEY }}
